name: Test

on:
  workflow_dispatch:
  pull_request:
    branches: ["main"]

jobs:
  license_finder:
    if: github.repository_owner == 'viamrobotics'
    name: Test
    runs-on: ubuntu-latest
    container:
      image: cimg/android:2024.01.1

    steps:
      - uses: actions/checkout@v4

      - name: Test
        run: ./gradlew test -x viam-core-sdk
